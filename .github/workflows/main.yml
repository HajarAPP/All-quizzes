name: 🏗️ Build & Upload SecurityPlus APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-apk:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: ☕ Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🐦 Set up Flutter (stable)
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 🔍 Flutter & Java version
        run: |
          flutter --version
          java -version

      - name: 📝 Write local.properties
        run: |
          FLUTTER_SDK="$(which flutter | xargs dirname | xargs dirname)"
          echo "flutter.sdk=$FLUTTER_SDK" > android/local.properties
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> android/local.properties
          cat android/local.properties

      - name: 🔧 Make Gradle wrapper executable
        run: chmod +x android/gradlew

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🏗️ Build Release APK
        run: |
          set -o pipefail
          flutter build apk --release --no-tree-shake-icons 2>&1 | tee build_output.log
          BUILD_EXIT=$?
          echo "BUILD_EXIT_CODE=$BUILD_EXIT" >> $GITHUB_ENV
          exit $BUILD_EXIT

      - name: 📋 Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build_output.log
          retention-days: 7
          if-no-files-found: ignore

      - name: 📤 Upload APK Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: SecurityPlus-Release-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
